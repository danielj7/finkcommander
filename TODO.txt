D = done
I = in progress

RELEASE 0.4.0

Target date: 09/07/2002

D Additional information for table:
	Installed version
	Binary version
	Stable version
	Unstable version
	
D Same information in package inspector

I Determinate progress bar and more detailed message text for fink command phases

Fix terminate command

Localizations:
 I	German
 I	French
	Spanish
		
D Collapse and expand output with double click (like Mail.app)

D Jaguar bug fixes


RELEASE 0.4.1

Target date: 11/15/2002

Security fixes (2 possible tracks):

	(1)	Use Security Framework:
		Write prototype for testing
		Test changes to Fink perl mods
		
	(2) Improve existing system:
		Option to run command in Terminal via NSAppleScript; FC would no longer touch password
		Default to not storing password in memory (will require re-entry if > 5 min. since last); Preferences:Commander option to store

New View menu item:  hide/show splitoffs

New command: update fink from cvs (raise warning alert)

Additional fink installation info:
	   last selfupdate-cvs 
	   last apt-get update

Command scheduling (e.g. selfupdate-cvs every 7 days) in preferences


RELEASE 0.4.2
	   
Command inspector: show currently running command, list of all packages subject to command (including those added by fink or apt-get) in a table showing the status of each (e.g. "compiled," "upacked"), when command started, how long command has been running; include refresh button

Dock icon customization: show status of command

Installed package browser

Show info panel for fink installation; mirror information in email sig

Customize sound

Better toolbar icons


LONG TERM:

Initial startup routine: selfupdate/selfupdate-cvs, apt-get update, scanpackages;
panel that steps through process

Custom script suite

Command to display a package's dependencies

Dependency browser

Try CamelBones again
