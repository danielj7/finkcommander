{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {openSelectedFiles = id; }; 
            CLASS = SBFileItemTree; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                loadingIndicator = NSProgressIndicator; 
                msgTextField = NSTextField; 
                outlineView = NSOutlineView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {
            CLASS = SBTreeWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                browser = NSBrowser; 
                loadingIndicator = NSProgressIndicator; 
                msgTextField = NSTextField; 
                outlineView = NSOutlineView; 
                tabView = NSTabView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}